<!-- res/drawable/rounded_button.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white"/>
    <corners android:radius="8dp"/> <!-- Bạn có thể thay đổi giá trị này để điều chỉnh độ bo tròn -->
    <stroke
        android:width="0.3dp"
        android:color="@color/primaryTextColor"/> <!-- Optional: Thêm viền cho button -->
</shape>
